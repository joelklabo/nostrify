name: CI Tests

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-test:
    name: Pytest Suite
    runs-on: ubuntu-latest
    continue-on-error: true
    strategy:
      matrix:
        lightningd-version: ["v23.02", "v22.11.1", "v0.12.1", "master"]

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Build Docker image
      run: docker build --build-arg LIGHTNINGD_VERSION=${{ matrix.lightningd-version }} -t cl-test .

    - name: Test Docker image
      run: docker run -v $(pwd):/build cl-test